import FeedbackDev from './feedbackDev';

export const metadata = {
    title: 'Send feedback to Developers | Evissa',
    description: 'Send feedback to Developers | Evissa',
};

const Contact = () => {
    return <FeedbackDev />;
};

export default Contact;
