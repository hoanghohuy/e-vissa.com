import ContactPage from './contactPage';

export const metadata = {
    title: 'Contacts | Admin E-vissa',
    description: 'Contacts | Admin E-vissa',
};

function page() {
    return (
        <>
            <ContactPage />
        </>
    );
}

export default page;
