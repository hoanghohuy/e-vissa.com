import UserPage from './userPage';

export const metadata = {
    title: 'Users | Admin E-vissa',
    description: 'Users | Admin E-vissa',
};

function page() {
    return <UserPage />;
}

export default page;
