import CategoryPage from './categoryPage';

export const metadata = {
    title: 'Category | Admin E-vissa',
    description: 'Category | Admin E-vissa',
};

function page() {
    return <CategoryPage />;
}

export default page;
