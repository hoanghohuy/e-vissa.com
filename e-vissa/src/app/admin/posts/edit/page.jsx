import EditPost from './editPost';

export const metadata = {
    title: 'Edit post | Admin E-vissa',
    description: 'Edit post | Admin E-vissa',
};

const page = () => {
    return (
        <>
            <EditPost />
        </>
    );
};

export default page;
