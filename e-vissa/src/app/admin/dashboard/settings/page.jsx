import SettingsPage from './settingsPage';

export const metadata = {
    title: 'Settings | Admin E-vissa',
    description: 'Settings | Admin E-vissa',
};

function page() {
    return <SettingsPage />;
}

export default page;
