import DashboardPage from './dashboardPage';

export const metadata = {
    title: 'Dashboard | Admin E-vissa',
    description: 'Dashboard | Admin E-vissa',
};

function page() {
    return <DashboardPage />;
}

export default page;
