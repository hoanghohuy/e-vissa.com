import CountryPage from './countryPage';

export const metadata = {
    title: 'Countries | Admin E-vissa',
    description: 'Countries | Admin E-vissa',
};

function page() {
    return <CountryPage />;
}

export default page;
