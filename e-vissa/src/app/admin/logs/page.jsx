import LogPage from './LogPage';

export const metadata = {
    title: 'Logs | Admin E-vissa',
    description: 'Logs | Admin E-vissa',
};

function page() {
    return <LogPage />;
}

export default page;
