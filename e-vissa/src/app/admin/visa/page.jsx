import VisaPage from './visaPage';

export const metadata = {
    title: 'VISA DETAIL | Admin E-vissa',
    description: 'VISA DETAIL | Admin E-vissa',
};

function page() {
    return <VisaPage />;
}

export default page;
